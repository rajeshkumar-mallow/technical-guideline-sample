# Preact Guidelines

**Reserved — not yet written.** This folder is for Preact-specific rules once a project on this stack needs them. Builds on [`../../shared/`](../../shared/README.md), [`../../js/`](../../js/README.md), and [`../`](../README.md) — and, since Preact is largely React-API-compatible (especially via `preact/compat`), likely reuses much of [`../react/`](../react/README.md) directly rather than restating it; this folder would mainly cover where Preact actually diverges (bundle size tradeoffs, `preact/compat` setup, missing RSC/Server Components support).

Add content here when a team's tech stack requires it, using the same Mandatory / Recommended / Anti-Patterns / Quick Reference template used elsewhere in this guideline.

Back to [`frontend/`](../README.md) · [root guideline](../../README.md).
