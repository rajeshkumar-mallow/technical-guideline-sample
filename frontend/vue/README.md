# Vue Guidelines

**Reserved — not yet written.** This folder is for Vue-specific rules once a project on this stack needs them. Follows the same structural pattern as [`../react/`](../react/README.md) + [`../next/`](../next/README.md) (framework fundamentals plus a meta-framework layer, if Nuxt is involved) — but Vue is not React-API-compatible, so it does **not** build on `../react/` itself. Builds on [`../../shared/`](../../shared/README.md), [`../../js/`](../../js/README.md), and [`../`](../README.md).

Add content here when a team's tech stack requires it, using the same Mandatory / Recommended / Anti-Patterns / Quick Reference template used elsewhere in this guideline.

Back to [`frontend/`](../README.md) · [root guideline](../../README.md).
